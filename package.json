{
  "name": "rspack-browserslist-target-env-bug",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "node scripts/patch-rspack.js",
    "build": "BROWSERSLIST=\"chrome>=109\" rspack build",
    "preview": "rspack preview"
  },
  "devDependencies": {
    "@rspack/cli": "^1.5.1",
    "@rspack/core": "^1.5.1",
    "typescript": "^5.9.2"
  }
}
